# 全球排行榜設定（Supabase，免費）

未設定時，排行榜會自動用**本機**（localStorage，只存這台裝置）。
要變成**全球共用**，照以下步驟接 Supabase（免費方案就夠）：

## 1. 建專案
1. 到 https://supabase.com 註冊、**New project**（記住 region、密碼）。
2. 進專案 → **Project Settings → API**，記下：
   - **Project URL**（例：`https://abcdxxxx.supabase.co`）
   - **anon public** key（一長串）

## 2. 建資料表
SQL Editor 貼上執行：
```sql
create table public.scores (
  id bigint generated by default as identity primary key,
  name text not null,
  score int not null,
  created_at timestamptz default now()
);
alter table public.scores enable row level security;

-- 允許任何人讀取與新增（休閒榜；不開 update/delete）
create policy "read" on public.scores for select using (true);
create policy "insert" on public.scores for insert with check (true);
```

## 3. 填入遊戲
編輯 `src/leaderboard.js` 最上面的 `CONFIG`：
```js
const CONFIG = {
  url: 'https://abcdxxxx.supabase.co',   // 你的 Project URL
  key: '貼上 anon public key',
  table: 'scores',
};
```
填好後 commit / push（或丟給我接）。重整後結局榜即為全球共用。

## 備註
- anon key 公開在前端是正常的；RLS 已限制只能讀取與新增，無法刪改他人分數。
- 怕灌票/髒話：可日後加 `name` 長度/字元檢查、或限制每 IP 寫入頻率（Supabase Edge Function）。需要再說。
- 分數＝通關效率：`打倒怪物數×1000 + (最多出牌數 − 實際出牌數)`，打越多怪、用越少出牌分越高。
